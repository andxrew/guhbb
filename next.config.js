/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = nextConfig

module.exports = {
	images: {
		domains: ["pbxt.replicate.delivery"], // Add the hostname(s) for your images here
	},
}
